//const backendUrl = "http://localhost:3000";
const backendUrl = "https://nutri-balance-three.vercel.app";

const userUrl = `${backendUrl}/api/user`;
const adminUrl = `${backendUrl}/api/admin`;

export { backendUrl, userUrl, adminUrl };

